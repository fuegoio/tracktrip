import { Album, Download, Rocket } from "lucide-react";
import { ReactNode } from "react";

export const icons: Record<string, ReactNode> = {
  Rocket: <Rocket />,
  Album: <Album />,
  Download: <Download />,
};
